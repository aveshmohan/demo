git config --global user.name "username of github"
git config --global user.email "email of github"


push commands
-------------
git init
git add
git remote add origin "url of github repo"
git commit -m "commit label"
git push -u origin master

second push
-----------
git add . 3
git commit -m "second push name"4
git push 5

branch creactions
----------------
git branch brancname 1
git checkout branchname 2

(git push -u origin branchname)


hello